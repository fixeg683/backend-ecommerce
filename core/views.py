# This file exists only as a safety stub.
# All views are defined directly in core/urls.py or in api/views.py.
# Having this file prevents ModuleNotFoundError if any stale import of
# `from .views import *` survives in core/urls.py across deployments.